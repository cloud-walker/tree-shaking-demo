{
  "scripts": {
    "clean": "rimraf build",
    "webpack": "webpack -p",
    "rollup": "rollup -c",
    "start": "npm run clean && npm run webpack && npm run rollup"
  },
  "private": true,
  "dependencies": {
    "babel-core": "^6.23.1",
    "babel-loader": "^6.3.0",
    "babel-preset-env": "^1.1.8",
    "babel-preset-es2015": "^6.22.0",
    "rimraf": "^2.5.4",
    "rollup": "^0.41.4",
    "rollup-plugin-uglify": "^1.0.1",
    "webpack": "^2.2.1"
  }
}
